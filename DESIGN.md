# N-Pang 정산 — 설계 문서

## 1. 배경 / 목적

기존에 `정산문서.xlsx`(엑셀)로 하던 모임 정산을 웹앱으로 옮긴다. 핵심 요구:

- 참가자와 회차별 총금액을 입력하고, **기여도(%)만 입력하면** 각자 낼 금액이 자동 계산된다.
- **주소(URL)만 주면** 참가자들이 결과를 볼 수 있어야 한다.
- 모바일에서도 봐야 한다 (카톡으로 링크 공유 → 폰에서 확인).

## 2. 핵심 설계 결정

| 결정 | 이유 |
|------|------|
| **순수 HTML/CSS/JS 단일 파일** | 빌드/의존성/서버 0. GitHub Pages에 푸시만 하면 즉시 배포 |
| **GitHub Pages (master / root)** | "주소만 주고 보여주기"에 최적, 무료 정적 호스팅 |
| **URL 해시로 결과 공유** | 서버·DB 없이 데이터를 링크에 담아 전달. 받는 사람은 주소만 열면 됨 |
| **localStorage 자동 저장** | 마지막 입력 내용 보존 (사용성). 초기화 버튼으로 리셋 |
| **원화(원)만 사용** | 엑셀의 `L열(동)`은 참고값이라 제외 |
| **100원 단위 절사** | 엑셀 원본 `TRUNC(...,-2)` 동작과 일치 |

## 3. 정산 로직

회차 `i`의 총금액 `Kᵢ`, 참가자 `j`의 회차 `i` 기여도 `pᵢⱼ(%)`:

```
정산액(j) = TRUNC( Σᵢ ( Kᵢ × pᵢⱼ / 100 ), 100원 )
```

구현 (`computeSettlement`, `truncTo`):

```js
function truncTo(value, unit) {
  // 엑셀 TRUNC(...,-2)와 동일(toward zero). 부동소수점 누적오차는 원 단위 반올림으로 보정.
  return Math.trunc(Math.round(value) / unit) * unit;
}
```

- 한 회차의 기여도 합은 100%가 되어야 한다 (UI 실시간 검증, 합계 ≠ 100이면 빨간 경고).
- 부동소수점 누적 오차로 절사 결과가 100원 어긋나는 경우를 막기 위해 절사 전 `Math.round`로 원 단위 보정.

## 4. 상태 구조

```js
state = {
  title: "8월 모임 정산",
  participants: ["고상태", "유현종", ...],   // 이름 배열
  rounds: [
    { place: "파일럿", amount: 121497, contribs: { 0: 25, 1: 25, ... } }  // contribs: 참가자 인덱스 → 기여도%
  ],
  pay: { bank: "우리은행", account: "1002-...", kakao: "..." },
  truncUnit: 100
}
```

- `contribs`의 키는 `participants` 배열의 **인덱스**. 참가자 삭제 시 `removeParticipant`가 키를 재인덱싱한다.

## 5. 공유 메커니즘

1. **인코딩** (`encodeState`): `JSON.stringify` → UTF-8 바이트 → `btoa` → URL-safe 변환(`+/=` → `-_`, 패딩 제거)
2. 결과 링크 = `location.origin + pathname + "#d=" + 인코딩값`
3. **디코딩** (`decodeState`): 역변환 → `JSON.parse` → `normalizeState`로 정규화
4. 페이지 로드 시 `init()`이 `#d=` 해시를 읽으면 **결과 보기 모드**로 시작

### 신뢰 경계 (`normalizeState`)

공유 링크와 localStorage는 **신뢰 불가 입력**이다 (사용자가 조작 가능). 디코딩 직후 타입/구조를 강제 정규화하여 조작된 페이로드로 인한 런타임 크래시(`participants`가 배열이 아닌 경우 등)를 막는다.

```js
participants: Array.isArray(obj.participants) ? obj.participants.map(String) : []
rounds: 배열 검증 + 각 round의 place/amount/contribs 타입 강제
```

## 6. 보안 / 개인정보

- **XSS**: 모든 사용자 입력 출력 경로에 `escapeHtml` 또는 `textContent` 사용.
- **개인정보**: 실제 계좌·실명이 든 `Ref-docs/정산문서.xlsx`는 `.gitignore`로 공개 저장소에서 제외. `index.html`에는 예시 placeholder만 존재.
- **공유 링크 노출**: 링크에 입력 내용이 인코딩되어 들어가므로, 민감정보는 신뢰하는 상대에게만 공유.

## 7. 검증 (엑셀 대조)

원본 엑셀 데이터(5명 × 5회차)로 계산 결과 일치 확인:

| 참가자 | 계산 결과 | 엑셀 |
|--------|-----------|------|
| 고상태 | 239,200원 | 239,200원 ✓ |
| 유현종 | 238,500원 | 238,500원 ✓ |
| 이용섭 | 0원 | 0원 ✓ |
| 정은경 | 215,600원 | 215,600원 ✓ |
| 차차 | 238,500원 | 238,500원 ✓ |
| **합계** | **931,800원** | **931,800원 ✓** |

추가 검증: 공유 링크 인코딩↔디코딩 라운드트립(한글 포함) 무손실, 실제 브라우저(데스크탑/모바일 375×812) 렌더링 콘솔 에러 0.
